services:
  api:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    env_file:
      - ../.env
    environment:
      DATABASE_PATH: /data/vasp_trace.db
    ports:
      - "8000:8000"
    volumes:
      - vasp_trace_data:/data

volumes:
  vasp_trace_data: